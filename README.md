# Hirra content calendar

Internal working dashboard, not a public Hirra surface. Deindexed on purpose (`robots.txt` + noindex meta) — unlisted GitHub Pages, never linked from any public site.

Source of truth for the underlying plan: `Hirra/Hirra-Content-Calendar.md` in the main project folder. This repo is just the visual rendering of it.

To update: edit `index.html` here, commit, push to `main`. GitHub Pages redeploys automatically in ~1-2 minutes.
